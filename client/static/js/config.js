// Points the UI at the server (gateway) deployment. Change this per environment
// (or replace with a build-time injected value) when deploying the client separately.
const API_BASE_URL = window.API_BASE_URL || "http://localhost:5000";
